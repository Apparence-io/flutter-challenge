name: CI Workflow

on:
  pull_request:
  push:

jobs:
  check-application:
    uses: Apparence-io/flutter-challenge/.github/workflows/check.yml@main

  build:
    needs: [check-application]

    runs-on: ubuntu-latest

    steps:
      - name: noop
        run: echo 'noop'
        
